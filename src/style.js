// Add common style variables here. Note that this must be written without ES6 syntax
// as it's loaded by the postcss config, which is not run through babel.
module.exports = {
  fontDisplay: "'Press Start 2P', monospace",
  fontText: "'Nanum Gothic Coding', monospace",
}
